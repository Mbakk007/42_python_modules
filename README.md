# 42 Python Modules

## Python Module 00
- Basic Python syntax: expressions, variables, basic types, and simple calculations.
- Writing functions and working with basic input/output.
- Conditions, loops, recursion..
- Intro to type annotations.

## Python Module 01
- Python program structure and execution (`if __name__ == "__main__":`).
- OOP fundamentals: classes, objects, attributes, and methods.
- Encapsulation: protecting data and validating updates (getters/setters).
- Inheritance: building simple class hierarchies and reusing code with `super()`.

## Python Module 02
- Exception handling fundamentals with `try/except` to avoid crashes.
- Common built-in exceptions and when they happen.
- Creating custom exception classes and catching them with inheritance.
- Raising errors for validation and using `finally` for guaranteed cleanup.

## Python Module 03
- Reading command-line arguments with `sys.argv` and doing basic stats.
- Basic data structures: lists, tuples, sets, and dictionaries.
- Understanding and implementing generators.

## Python Module 04
- File I/O basics: reading and writing text files safely.
- Understanding file modes (`r`, `w`) and what they do.
- Using context managers (`with`) to ensure files are closed properly.
- Handling file-related errors cleanly.
- Basic stream separation: stdin/stdout/stderr for normal output vs errors.

## Python Module 05
- Abstract base classes (ABC) and method overriding.
- Polymorphism: same method calls working across different subclasses.

## Python Module 06
- Packages and modules: what `__init__.py` does and how it affects imports.
- Different import styles (`import`, `from ... import`, `import ... as`) and when to use them.
- Absolute vs relative imports inside a package structure.
- Avoiding circular imports using late imports.

## Python Module 07
- Abstract base classes.
- Polymorphism.
- Multiple inheritance.

## Python Module 08
- Detecting virtual enviroments.
- Dependency management basics (pip and Poetry).
- Configuration with environment variables and `.env` files.
- Simple “real-world” project structure: `.gitignore`, `.env.example`, requirements files.

## Python Module 09
- Pydantic models (v2): types and `Field` constraints.
- Automatic parsing.
- Custom validation with `@model_validator`.

## Python Module 10
- Lambdas and functional tools (`map`, `filter`, `sorted`) for simple transformations.
- Higher-order functions (functions that take/return other functions).
